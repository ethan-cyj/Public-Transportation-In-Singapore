_HiStOrY_V2_
install.packages("tidytuesdayR")
tuesdata <- tidytuesdayR::tt_load('2023-01-10')
install.packages("tidytuesdayR")
install.packages("tidytuesdayR")
tuesdata <- tidytuesdayR::tt_load('2021-04-20')
tuesdata
install.packages("tidytuesdayR, vroom")
install.packages("tidytuesdayR", "vroom")
install.packages("tidytuesdayR", "vroom")
install.packages(c("tidytuesdayR", "vroom"))
install.packages(c("tidytuesdayR", "vroom"))
install.packages(c("tidytuesdayR", "vroom"))
install.packages(c("tidytuesdayR", "vroom"))
tuesdata <- tidytuesdayR::tt_load('2021-04-20')
tuesdata <- tidytuesdayR::tt_load('2021-04-20')
R.version.string
install.packages("otpr")
otp_connect(
hostname = "localhost",
router = "default",
port = 8080,
tz = Sys.timezone(),
ssl = FALSE,
check = TRUE
)
library(otpr)
otp_connect(
hostname = "localhost",
router = "default",
port = 8080,
tz = Sys.timezone(),
ssl = FALSE,
check = TRUE
)
otp_connect(
hostname = "localhost",
router = "default",
port = 8080,
tz = Sys.timezone(),
ssl = FALSE,
check = TRUE
)
otp_connect(
hostname = "localhost",
router = "default",
port = 8080,
tz = Sys.timezone(),
ssl = FALSE
)
getwd
getwd()
setwd("/Users/ethan/Documents/GitHub/Public-Transportation-In-Singapore/Backend/SP3")
library(dplyr)
locations = read.csv("../../data/Isochrone_data/mrt_station_colours.csv")
View(locations)
location1 = c(locations$Latitude[1], locations$Longitude[1])
otp_get_isochrone(
otpcon,
location1,
fromLocation = TRUE,
format = "JSON",
mode = "TRANSIT",
date = format(Sys.Date(), "%m-%d-%Y"),
time = format(Sys.time(), "%H:%M:%S"),
cutoffs,
batch = TRUE,
arriveBy = FALSE,
maxWalkDistance = NULL,
walkReluctance = 2,
waitReluctance = 1,
transferPenalty = 0,
minTransferTime = 0,
extra.params = list()
)
#run OTP in command line first, then check connection
otpcon = otp_connect(
hostname = "localhost",
router = "default",
port = 8080,
tz = Sys.timezone(),
ssl = FALSE
)
otp_get_isochrone(
otpcon,
location1,
fromLocation = TRUE,
format = "JSON",
mode = "TRANSIT",
date = format(Sys.Date(), "%m-%d-%Y"),
time = format(Sys.time(), "%H:%M:%S"),
cutoffs,
batch = TRUE,
arriveBy = FALSE,
maxWalkDistance = NULL,
walkReluctance = 2,
waitReluctance = 1,
transferPenalty = 0,
minTransferTime = 0,
extra.params = list()
)
cutoffs = c(900, 1800, 2700)
otp_get_isochrone(
otpcon,
location1,
fromLocation = TRUE,
format = "JSON",
mode = "TRANSIT",
date = format(Sys.Date(), "%m-%d-%Y"),
time = format(Sys.time(), "%H:%M:%S"),
cutoffs,
batch = TRUE,
arriveBy = FALSE,
maxWalkDistance = NULL,
walkReluctance = 2,
waitReluctance = 1,
transferPenalty = 0,
minTransferTime = 0,
extra.params = list()
)
